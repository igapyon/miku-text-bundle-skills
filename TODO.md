# TODO

- Inspect the upstream README and public CLI/API/runtime contract in detail.
- Add bundle contents verification once a distributable bundle layout is introduced.
- Compare with a similar local `-skills` sister project checkout under `workplace/` when available.
